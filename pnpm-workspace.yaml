packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 blocks dependency build/postinstall scripts by default.
# Explicitly allow the native toolchains the build genuinely needs.
onlyBuiltDependencies:
  - "@biomejs/biome"
  - "@tailwindcss/oxide"
  - "esbuild"
  - "sharp"
  - "unrs-resolver"
