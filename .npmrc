auto-install-peers=true
strict-peer-dependencies=false
# Bleeding-edge majors (React 19 / Next 16 / Tailwind 4) advertise tight peer
# ranges; don't let a peer mismatch block install while the ecosystem catches up.
